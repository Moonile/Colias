#include <iostream>
#include <opencv2/opencv.hpp>

using namespace cv;

int main(){
	class photoreceptor
	public:
	double elem1;
		double elem2;
		double magnitude() const; {
			double res = sqrt(elem1 * elem1 + elem2 * elem2);
			return res;
		};
}